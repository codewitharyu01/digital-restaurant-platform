[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["cd myrestu && python -m pip install -r requirements.txt"]

[phases.build]
cmds = ["cd myrestu && python manage.py collectstatic --noinput && python manage.py migrate --noinput"]

[start]

cmd = "cd myrestu && gunicorn myrestu.wsgi"